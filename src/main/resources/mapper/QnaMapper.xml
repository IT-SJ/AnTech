<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ans.antech.mapper.QnaMapper">

  <!-- 사용자 ID로 질문 찾기 (SELECT) -->
  <select id="findQnaByUserId" resultType="com.ans.antech.model.Qna">
    SELECT idx, title, text, reply, id, dt
    FROM ANS_QNA
    WHERE id = #{userId}
    ORDER BY dt DESC
  </select>

  <!-- 질문 추가 (INSERT) -->
  <insert id="insertQna" resultType="com.ans.antech.model.Qna"> <!-- resultType 추가 -->
        INSERT INTO ANS_QNA (TITLE, TEXT, ID, DT)
        VALUES (#{title}, #{text}, #{id} NOW())
  </insert>

  <!-- 질문 목록 조회 (SELECT) -->
  <select id="getAllQna" resultType="com.ans.antech.model.Qna">
        SELECT * FROM ANS_QNA ORDER BY DT DESC
  </select>
</mapper>